# Changelog

## 0.2.0 — 首个公开发布版本

### Added

- **可视化撤销面板**：侧栏底部「撤销」入口（未撤销计数徽章）→ 审查面板——历史列表（时间 / 工具徽章 / 文件名 / `+恢复 −移除` 行数 / 轮次徽章）+ 撤销视角红绿 diff + 两阶段撤销按钮
- **两阶段撤销与安全闸**：自动预检逐条给原因（`external_modified` / `superseded_by_later_ops` / `file_creation` / `already_reverted` / `file_missing` / `stale` 等），写回携带 sha256 乐观锁；创建类撤销真删文件（内容/hash 双闸 + 确认）
- **回退到此状态（RewindTo）**：选中任意历史条目单次原子写回，链条咬合校验 + 明示将丢弃的后续步数，其后条目批量标记 cascade
- **级联恢复**：重新应用上游条目时整批复活被连带作废的行（确认框明示「连带恢复 N 条」），undo/redo 双向幂等
- **会话切换与自动跟随**：面板自动跟随当前查看的会话（betterSidebar 主源 + sessions.list 兜底），可在切换器中钉住同项目树会话（带平台标题）；host 侧逐请求核验，伪造会话 id 一律 404
- **轮次徽章**：从平台轨迹读取工具调用的权威轮次/步号，快照映射回真实模型轮次
- **失败日志**：失败的调用同样进面板——试过什么、为什么没成，一目了然
- **聊天命令**：`/undo`、`/undo list`、`/undo <n>`、`/undo sessions`、`/undo prune [days]`、`/undo git [n]`、`/undo git-status`
- **工作区归一**：会话 cwd 规范化后派生存储键——同一目录无论路径写法如何，恒为同一个快照仓库（存量目录零迁移）

### Changed

- diff 渲染与计算解耦：host 侧 LCS（前缀/后缀裁剪 + 整数行 ID + Uint32 DP）产出 hunk，客户端按行类型纯 CSS 上色（主题令牌混色）
- 快照存储按「工作区 × 会话」两层隔离：append-only JSONL（单飞写锁 + 原子改名），git 归档第二层，7 天惰性清理

### Fixed

- 外部修改检测：快照后文件被手动编辑的条目拒绝盲目撤销（fail-closed），引导走回退或强制通道
- 快照账本健壮性：同毫秒编辑漏计、失败调用错位配对、旧格式条目兼容标注「改后状态未捕获」
